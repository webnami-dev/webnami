---
layout: "base.njk"
pagination:
  data: collections.paginatedPosts
  size: 1
  alias: postsPage
permalink: "{% if postsPage.page == 1 %}/{% else %}/posts/{{ postsPage.page }}/{% endif %}"
eleventyComputed:
  title: "{% if postsPage.page == 1 %}{{ config.homepage.metadata.title }}{% else %}Posts - Page {{ postsPage.page }}{% endif %}"
  description: "{% if postsPage.page == 1 %}{{ config.homepage.metadata.description }}{% else %}Posts page {{ postsPage.page }}{% endif %}"
  img: "{{ config.homepage.metadata.img }}"
---

{% import "macros/homepage.njk" as page with context %}
{{ page.renderHomePage(postsPage) }}

