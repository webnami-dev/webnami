{% extends "base.njk" %}

{% block content %}
  <div class="flex flex-col items-center justify-center min-h-[calc(100vh-3.5rem-3rem)]">
    <h1 class="text-5xl font-semibold text-content mb-6">{{ greeting }}</h1>

    <div class="flex gap-3">
      <a href="/admin/posts/new" class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-primary text-white text-sm font-medium hover:bg-primary/80 transition-colors">
        <i data-lucide="plus" class="w-4 h-4"></i>
        New Post
      </a>
      <a href="/admin/pages/new" class="inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-border text-content text-sm font-medium hover:bg-hover transition-colors">
        <i data-lucide="plus" class="w-4 h-4"></i>
        New Page
      </a>
    </div>
  </div>
{% endblock %}
